import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Ibadat Refrigeration - AC Repair & Service in Malkapur",
  description: "Professional air conditioning repair, refrigeration sales, and appliance service in Malkapur, Buldhana. Open 24/7 for emergency repairs. Call +91 95298 25994",
  keywords: "AC repair, Refrigeration service, Air conditioning repair Malkapur, Water purifier repair, Washing machine service",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
